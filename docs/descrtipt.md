dự án của tôi flutter(chỉ làm frontend và consume api từ backend đã có sẵn) là 1 sass quản lý nhà hàng sẽ có super admin và các store user, super admin thì sẽ quản lý các store, user, gói dịch vụ, doanh thu từ hệ thống. store user thì sẽ có các role trong quy trình quản lý nhà hàng(abtract) 1 store user có thể thuộc về một hoặc nhiều store với mỗi role ở mỗi store là khác nhau và có thể linh hoạt chuyển qua lại các store store. Mỗi sotre sẽ có các role như Onwner, Manager, Staff, Kitchen. account sẽ có type là StoreUser vá SuperAdmin đê phân biệt, ở mỗi store thì StoreUser sẽ có các role tương ứng đã mô tả bên trên.